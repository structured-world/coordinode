# cargo-deny configuration for CoordiNode.
# Run: cargo deny check licenses

[licenses]
# Allow these licenses (CE is AGPL-3.0, dependencies must be compatible)
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "BSL-1.0",
    "MPL-2.0",
    "AGPL-3.0-only",
    "CC0-1.0",
    "CDLA-Permissive-2.0",
]
confidence-threshold = 0.8

[licenses.private]
ignore = true

[advisories]
# Ignore unmaintained `encoding` crate — transitive dep from lindera (CJK tokenization).
# No alternative available; lindera upstream tracks this.
ignore = ["RUSTSEC-2021-0153"]

[bans]
multiple-versions = "allow"
wildcards = "allow"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = ["https://github.com/structured-world/rust-stemmers"]
