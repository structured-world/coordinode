[workspace]
# No crates.io publishing — coordinode is a server binary, not a library.
# Releases produce musl binaries + Docker image pushed to ghcr.io.
semver_check = false
git_only = true
publish = false
# Each crate owns its own CHANGELOG.md and is versioned independently, so
# release-plz bumps only crates whose commits actually touched them.
# Default per-crate tag pattern `{{ package }}-v{{ version }}` avoids collisions
# with the product tag `v{{ version }}` (owned by coordinode-server below).
git_tag_enable = false
git_release_enable = false

[changelog]
commit_parsers = [
    { message = "^chore", skip = true },
    { message = "^ci", skip = true },
    { message = "^style", skip = true },
    { message = "^build", skip = true },
    { message = "^docs\\(ci\\)", skip = true },
    { message = "^docs\\(copilot\\)", skip = true },
    { message = "^Merge", skip = true },
    { message = "^Revert", skip = true },
    { message = "^docs", group = "Documentation" },
    { message = "^feat", group = "Added" },
    { message = "^fix", group = "Fixed" },
    { message = "^perf", group = "Performance" },
    { message = "^refactor", group = "Refactored" },
    { message = "^test", group = "Testing" },
]

# The shipped binary — the only crate that gets a product-style git tag
# (`v{{ version }}`) and a GitHub Release. Its version is the "product version".
[[package]]
name = "coordinode-server"
git_tag_enable = true
git_release_enable = true
git_tag_name = "v{{ version }}"

# Test-only crate — not a releasable package. Excluded from release-plz
# processing to avoid "package not found in workspace" errors: release-plz
# checks out an old baseline tag where this crate did not yet exist.
[[package]]
name = "coordinode-integration"
release = false
